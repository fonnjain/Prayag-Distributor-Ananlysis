# Replit Prompt 86 — Fix what the guards found, and what they cannot see

**Date:** 13 September 2026
**Follows:** the guard failure analysis, whose classification is accepted.
**Scope:** Six items, in priority order. A and B are the substantive ones.

**Standing rule:** every figure states which database or source it came from.

---

## Priority, and one change from the report's order

The report ranks August SKU first and the broken guard runner second. **I would
reverse them.**

A guard that fails loudly is visible. **Two guards that evaluate nothing are
invisible**, and they have been since 23 August — three weeks in which the Growth
Report contract, the ACTIVATE/WIDEN basis, the lever precedence and every scheme
check have been unverified while appearing to run.

Including *"clarification-only ₹15 lakh slabs are never recommended"*, which is a
money control.

---

## A — the guard runner evaluates nothing

```
Code change. Show the diff before applying.

growth-report-guards and schemes-nudge-guards both fail with:

  Cannot find module artifacts/api-server/dist/thread-stream-worker.mjs

The disposable API server exits before readiness, so NO business assertion
runs. Current builds emit index.mjs, pino-worker.mjs, pino-file.mjs and
pino-pretty.mjs - not thread-stream-worker.mjs.

Cause: parallel workflows delete and rebuild the same dist directory
between the guard's readiness check and process startup.

A1. Fix the root cause, not the symptom. Either:
      isolate each guard's build output so parallel workflows cannot
      destroy it, or
      run guards against a pinned, already-built, reachable server

    Recommend one and say why.

A2. THE RUNNER MUST FAIL LOUDLY WHEN IT CANNOT EVALUATE.

    A guard that exits before asserting anything must report
    "could not evaluate" as a distinct outcome - never silence, never a
    pass. This is the same principle as the audit manifest: a check that
    cannot run must say so.

A3. Once fixed, RUN BOTH and print every assertion result. Three weeks
    have passed unverified. I want to know whether anything is actually
    broken underneath, particularly:

      ACTIVATE and WIDEN secondary-retailer basis - note Prompt 75B
        corrected a mixed-population denominator here
      CLOSE -> RECOVER -> ACTIVATE -> WIDEN precedence
      no conflicting lever per entity
      clarification-only Rs 15 lakh slabs never recommended
      every nudge uses a known Q2 scheme

A4. When did each last actually evaluate successfully? If the answer is
    23 August, say so plainly.
```

---

## B — August SKU: production has zero

```
Read-only first.

  Development Aug-26   8,602 rows   26.6% of Aug-25
  Production Aug-26        0 rows   -100% against a 19,417 floor

B1. Confirm production Aug-26 secondary_sku_line: rows, value, frozen_at.
    The four-month window means it should be OPEN until 1 December - not
    frozen.

B2. Development has 8,602 rows that production does not. Where did they
    come from, and are they the Product-Wise order booking file loaded into
    the wrong table in development?

    If so, development is wrong and production is merely empty. Those are
    different problems and only one needs fixing.

B3. THE AGGREGATE CHECK MASKS IT. 123,326 actual against a 105,264 floor
    passes because April-July cover for August.

    An aggregate that hides a -100% month is a weak control. Recommend
    whether the aggregate should fail when any constituent month fails.

B4. What is needed to load August properly? The month is open, so the
    normal loader should accept the retailer and item-level export when
    Prayag send it.

Report before loading anything.
```

---

## C — the typecheck defect

```
Code change.

src/widgets/ImportedChart.tsx line 78, two TS18047 errors - "total is
possibly null" in the percent-stack scale expression.

The reducer uses number | null source values and its inferred accumulator
type is not narrow enough for total > 0 and 100 / total.

Fix the typing. Introduced 11 September, before today's publishes.
```

---

## D — stale report_logic anchors

```
Code change. Update two expectations.

The manifest exposed these correctly. They are stale, not broken:

  6.2a  expected PTMT LY Rs 45,54,928
        actual            Rs 44,20,513.89
        difference        Rs 1,34,414.11 - EXACTLY the separately
                          classified CISTERN

  6.6a  expected Universal PTMT LY 5,106 pieces
        actual                      5,006 pieces
        difference                    100 pieces - EXACTLY CISTERN

Both anchors predate the PTMT/CISTERN split. Update them to the current
classification.

State in the check's own comment that the anchor was updated on
13 September to reflect the CISTERN split, so the next person does not
read the change as tampering.
```

---

## E — retired manifest keys

```
Three checks are permanently not-evaluated:

  E1_name_match_pct
  E2_unmatched_names
  E3_fy2627_duplicates

All belong to retired Target Master name-matching. Either remove them from
the expected manifest, or mark them formally non-applicable with a reason.

A permanently not-evaluated check is the same problem the manifest was
built to solve, one level up.
```

---

## F — a passing test on a degraded path

```
Read-only.

The distributor-tab tests logged "column sl.qty does not exist" while
loading category multipliers, and still PASSED.

F1. What is that query, and what does it return when the column is missing
    - empty, null, or a default?

F2. If a multiplier query fails and the test passes anyway, the test is not
    testing the multiplier. Print what it actually asserts.

F3. Does sl.qty exist under another name? The tank work established that
    sale_line holds qty and qty_ltr. If the query references a column that
    was renamed, production may be affected too - check whether this query
    runs anywhere outside tests.

A test that passes while its query silently fails is worse than a failing
test.
```

---

## Notes

- **A is first because invisibility beats visibility as a risk.** Two guards have
  been reporting nothing for three weeks while appearing to run. A2 matters as
  much as A1 — the runner must distinguish "evaluated and passed" from "could not
  evaluate", exactly as the audit manifest now does.
- **A3 is the part I would read closely.** Prompt 75B corrected a mixed-population
  denominator in ACTIVATE and WIDEN. Those guards have not verified anything since
  before that change.
- **B2 may invert the problem.** If development's 8,602 rows are the Product-Wise
  order booking file in the wrong table, then development is wrong and production
  is simply empty — and the fix is to development, not production.
- **F is small and nasty.** A green test exercising a broken query gives false
  assurance, which is worse than no test.

## What is passing, and worth recording

**The date-signature guard is clean** across all four historical years.

**`frozen_drift_check` and Audit Group 10 pass** — all three closed years reconcile
exactly to their anchors: FY2023-24 ₹349.023 Cr, FY2024-25 ₹341.143 Cr, FY2025-26
₹360.995 Cr, with residuals in the ninth decimal place.

**And the manifest worked.** All eight `report_logic` checks now evaluate, and the
two failures are stale anchors rather than broken logic. That is precisely what it
was built to expose.